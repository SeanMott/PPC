#include <PPCAnalyse/ASM/Stage1/Stage1ASM.hpp>

void PPC::Analyse::ASM::Stage1::PerformStage1(const std::string& code)
{
	//searches through the code till it runs into the start of a func, obj, or sym

	//goes into it reaches the end of the func, obj, or sym and stores it away

	//write each define to a disc
}