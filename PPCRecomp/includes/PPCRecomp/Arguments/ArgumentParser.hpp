#pragma once

//defines a parser for arguments

#include <PPCRecomp/Arguments/Arguments.hpp>
#include <PPCRecomp/Arguments/ArgumentSettings.hpp>

namespace PPC::Recomp::Arguments
{
	//parses the arguments
	static inline ArgumentSettings ParseArguments(const int args, const char* argv[])
	{
		ArgumentSettings settings;


		return settings;
	}
}