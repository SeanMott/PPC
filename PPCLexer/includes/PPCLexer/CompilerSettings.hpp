#pragma once

//defines various settings for this lexer

namespace PPC::Settings
{
	//defines compiler settings
	struct CompilerSettings
	{
		//strips out all the comments from the token stream in Subpass 2
		//output each function and struct into it's own file for Tokenstream and psudo C++
		//output psudo-C++
	};
}